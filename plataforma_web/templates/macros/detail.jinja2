{%- macro page(label) -%}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">{{ label }}</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <div class="btn-group me-2">
                <button type="button" class="btn btn-sm btn-outline-secondary">Share</button>
                <button type="button" class="btn btn-sm btn-outline-secondary">Export</button>
            </div>
        </div>
    </div>
{%- endmacro -%}

{%- macro page_breadcrumb(label) -%}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">{{ caller () }}{{ label }}</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <div class="btn-group me-2">
                <button type="button" class="btn btn-sm btn-outline-secondary">Share</button>
                <button type="button" class="btn btn-sm btn-outline-secondary">Export</button>
            </div>
        </div>
    </div>
{%- endmacro -%}

{%- macro page_breadcrumb_item(label, link='') -%}
    <a href="{{ link }}">{{ label }}</a> /
{%- endmacro -%}

{%- macro topbar_actions() -%}
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <span class="d-none d-md-block">Acciones <i class="fa fa-angle-down"></i></span>
            <span class="d-block d-md-none"><i class="fa fa-plus"></i></span>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            {{ caller () }}
        </div>
    </li>
{%- endmacro -%}

{%- macro topbar_actions_option(label, url) -%}
            <a class="dropdown-item" href="{{ url }}">{{ label }}</a>
{%- endmacro -%}

{%- macro topbar_option(label, url, icon) -%}
    <li class="nav-item">
        <a class="nav-link" href="{{ url }}"><i class="{{ icon }}"></i> {{ label }}</a>
    </li>
{%- endmacro -%}

{%- macro topbar_option_edit(label, url) -%}
    <li class="nav-item">
        <a class="nav-link" href="{{ url }}"><i class="fas fa-edit"></i> {{ label }}</a>
    </li>
{%- endmacro -%}

{%- macro topbar_option_delete(label, url) -%}
    <li class="nav-item">
        <a class="nav-link" href="{{ url }}"><i class="fas fa-trash"></i> {{ label }}</a>
    </li>
{%- endmacro -%}

{%- macro topbar_option_recover(label, url) -%}
    <li class="nav-item">
        <a class="nav-link" href="{{ url }}"><i class="fas fa-recycle"></i> {{ label }}</a>
    </li>
{%- endmacro -%}

{%- macro card(title='', estatus='') -%}
    {% if estatus == 'B' %}
    <div class="card text-white bg-dark mb-3">
    {% else %}
    <div class="card mb-3">
    {% endif %}
        <div class="card-body">
            {% if title %}<h5 class="card-title">{{ title }}</h5>{% endif %}
            {{ caller () }}
        </div>
    </div>
{%- endmacro -%}

{%- macro label_value(label, value, link='') -%}
    {% if value != None and value != '' %}
        <div class="row">
            <div class="col-md-3 text-right">
                {{ label }}
            </div>
            <div class="col-md-9">
                {% if link %}
                <strong><a href="{{ link }}">{{ value }}</a></strong>
                {% else %}
                <strong>{{ value }}</strong>
                {% endif %}
            </div>
        </div>
    {% endif %}
{%- endmacro -%}

{%- macro label_value_new_tab(label, value, link) -%}
    {% if value != None and value != '' %}
        <div class="row">
            <div class="col-md-3 text-right">
                {{ label }}
            </div>
            <div class="col-md-9">
                <strong><a href="{{ link }}" target="_blank">{{ value }} <i class="fas fa-external-link-alt"></i></a></strong>
            </div>
        </div>
    {% endif %}
{%- endmacro -%}

{%- macro label_value_big(label, value, link='') -%}
    {% if value != None and value != '' %}
        <div class="row">
            <div class="col-md-3 text-right">
                <!-- No se muestra el label -->
            </div>
            <div class="col-md-9">
                {% if link %}
                <h3><a href="{{ link }}">{{ value }}</a></h3>
                {% else %}
                <h3>{{ value }}</h3>
                {% endif %}
            </div>
        </div>
    {% endif %}
{%- endmacro -%}

{%- macro label_value_boolean(label, value) -%}
    <div class="row">
        <div class="col-md-3 text-right">
            {{ label }}
        </div>
        <div class="col-md-9">
            {% if value %}<strong>Sí</strong>{% else %}No{% endif %}
        </div>
    </div>
{%- endmacro -%}

{%- macro button_for_modal(label, id) -%}
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#{{ id }}">
        {{ label }}
    </button>
{%- endmacro -%}

{%- macro modal(title, message, id, url) -%}
    <div class="modal fade" id="{{ id }}" tabindex="-1" aria-labelledby="{{ id }}Label" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="{{ id }}Label">{{ title }}</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    {{ message }}
                </div>
                <div class="modal-footer">
                    <a class="btn btn-secondary" href="#" data-dismiss="modal">Cancelar</a>
                    <a class="btn btn-primary" href="{{ url }}">{{ title }}</a>
                </div>
            </div>
        </div>
    </div>
{%- endmacro -%}

{%- macro custom_javascript_delete_recover() -%}
    <script>
        function showModalDelete() { $('#modalDelete').modal() }
        function showModalRecover() { $('#modalRecover').modal() }
    </script>
{%- endmacro -%}

{# JavaScript Datatable #}
{%- macro datatable(table_id, order="asc") -%}
    <script>
        $('#{{ table_id }}').DataTable({
            {% if order == "asc" %}order: [[ 0, "asc" ]],{% endif %}
            {% if order == "desc" %}order: [[ 0, "desc" ]],{% endif %}
            language: {
                lengthMenu: "Mostrar _MENU_",
                search: "Filtrar:",
                zeroRecords: "No se encontraron registros",
                info: "Página _PAGE_ de _PAGES_",
                infoEmpty: "No hay registros",
                infoFiltered: "(_TOTAL_ filtrados de _MAX_ registros)",
                oPaginate: {
                    sFirst: "Primero",
                    sLast: "Último",
                    sNext: "Siguiente",
                    sPrevious: "Anterior"
                }
            }
        });
    </script>
{%- endmacro -%}
