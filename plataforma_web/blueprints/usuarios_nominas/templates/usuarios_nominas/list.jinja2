{% extends 'layouts/app.jinja2' %}
{% import 'macros/list.jinja2' as list %}
{% import 'macros/topbar.jinja2' as topbar %}

{% block title %}Usuarios Nóminas{% endblock %}

{% block custom_head %}
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
{% endblock %}

{% block topbar_actions %}
    {% call topbar.page_buttons(titulo) %}
    {% endcall %}
{% endblock %}

{% block content %}
    {% call list.card() %}
        <table id="usuarios_nominas" class="table {% if estatus == 'B'%}table-dark{% endif %} display nowrap" style="width:100%">
            <thead>
                <tr>
                    <th>Fecha - Quincena</th>
                    <th>Descargar PDF</th>
                    <th>Descargar XML</th>
                </tr>
            </thead>
        </table>
    {% endcall %}
{% endblock %}

{% block custom_javascript %}
    {{ list.config_datatable() }}
    <script>
        configDataTable['ajax']['url'] = '/usuarios_nominas/datatable_json';
        configDataTable['ajax']['data'] = {{ filtros }};
        configDataTable['columns'] = [
            { data: 'fecha' },
            { data: 'url_pdf' },
            { data: 'url_xml' }
        ];
        configDataTable['columnDefs'] = [
            {
                targets: 1, // url_pdf
                data: null,
                render: function(data, type, row, meta) {
                    return '<a href="' + data + '" target="_blank"><i class="fs-2 bi bi-filetype-pdf" style="color: red;"></i></a>';
                }
            },
            {
                targets: 2, // url_xml
                data: null,
                render: function(data, type, row, meta) {
                    return '<a href="' + data + '" target="_blank"><i class="fs-2 bi bi-filetype-xml" style="color: teal;"></i></a>';
                }
            }
        ];
        $('#usuarios_nominas').DataTable(configDataTable);
    </script>
{% endblock %}
