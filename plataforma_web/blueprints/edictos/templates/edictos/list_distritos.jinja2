{% extends 'layouts/app.jinja2' %}
{% import 'macros/list.jinja2' as list with context %}

{% block title %}Edictos - Distritos{% endblock %}

{% block topbar_actions %}
    {{ list.topbar_option_search('Buscar', url_for('edictos.search')) }}
{% endblock %}

{% block content %}
    {% call list.page_breadcrumb('Distritos') %}
        {{ list.page_breadcrumb_item('Edictos', link=url_for('edictos.list_active')) }}
    {% endcall %}
    <div class="container-fluid">
        {% call list.card() %}
            <table class="table">
                <thead>
                    <tr>
                        <th>Distritos</th>
                    </tr>
                </thead>
                <tbody>
                    {% for distrito in distritos %}
                    <tr>
                        <td><a href="{{ url_for('edictos.list_autoridades', distrito_id=distrito.id) }}">{{ distrito.nombre }}</a></td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% endcall %}
    </div>
{% endblock %}
